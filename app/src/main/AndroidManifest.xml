<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Focus"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".acitivity.ActivityGoals"
            android:exported="false" />
        <activity
            android:name=".acitivity.ActivityStats"
            android:exported="false" />
        <activity
            android:name=".acitivity.ActivityProfile"
            android:exported="false" /> <!-- LOGIN = TELA INICIAL -->
        <activity
            android:name=".acitivity.ActivityLogin"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- OUTRAS TELAS -->
        <activity
            android:name=".acitivity.ActivityRegister"
            android:exported="false" />
        <activity
            android:name=".acitivity.MainActivity"
            android:exported="false" />
        <activity android:name=".acitivity.ActivityEditProfile"
            android:exported="false" />
    </application>

</manifest>